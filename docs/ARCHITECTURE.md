#   `obs-shards` architecture
The applications are developed using PostgreSQL as the relational database and Django as the Web framework. The frontend is being developed using React and Next.js as the frameworks. The project uses Docker and docker-compose to ensure reproducible development and deployment environments.

##  `notes` application
The `notes` application 